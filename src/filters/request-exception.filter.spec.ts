import { RequestExceptionFilter } from './request-exception.filter';

describe('RequestExceptionFilter', () => {
  it('should be defined', () => {
    expect(new RequestExceptionFilter()).toBeDefined();
  });
});
